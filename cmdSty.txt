app-commodity {
  // Remove ALL scroll from the select container itself
  ng-select.ng-select-multiple {
    .ng-select-container {
      height: auto !important;
      max-height: none !important;
      overflow: visible !important;
    }

    .ng-value-container {
      flex-wrap: wrap;
      overflow: visible !important;
      padding: 4px 8px;
      gap: 2px;
    }

    .ng-value {
      font-size: 11px;
      max-width: 180px;
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
    }

    // Only the DROPDOWN PANEL gets scroll, nothing else
    .ng-dropdown-panel {
      position: absolute !important;
      z-index: 1050;
      width: 260px;
      
      .ng-dropdown-panel-items {
        max-height: 150px !important;
        overflow-y: auto !important;
        overflow-x: hidden !important;
      }

      .ng-option {
        font-size: 12px;
        padding: 6px 8px;
        white-space: normal;
        word-break: break-word;
      }
    }
  }
}
