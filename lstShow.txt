import React from 'react';
import {
  Container,
  Box,
  Typography,
  Grid,
  Paper,
  Button,
  TextField,
} from '@mui/material';
import { DataGrid, GridColDef } from '@mui/x-data-grid';
import DownloadIcon from '@mui/icons-material/Download';
import UploadFileIcon from '@mui/icons-material/UploadFile';
import IconButton from '@mui/material/IconButton';
import LinearProgress from '@mui/material/LinearProgress';

const quoteComparisons = [
  { id: 1, name: 'Comparison #4', dateCreated: '45% Complete' },
  { id: 2, name: 'Comparison #3', dateCreated: 'January 21, 2024' },
  { id: 3, name: 'Comparison #2', dateCreated: 'January 20, 2024' },
  { id: 4, name: 'Comparison #1', dateCreated: 'January 19, 2024' },
];

const columns: GridColDef[] = [
  { field: 'name', headerName: 'Name', width: 200 },
  {
    field: 'dateCreated',
    headerName: 'Date Created',
    width: 200,
    renderCell: (params) => (
      params.value === '45% Complete' ? (
        <Box width="100%">
          <LinearProgress variant="determinate" value={45} />
          <Typography variant="body2" color="textSecondary" align="center">
            45% Complete
          </Typography>
        </Box>
      ) : (
        params.value
      )
    ),
  },
  {
    field: 'download',
    headerName: 'Download',
    width: 100,
    renderCell: () => (
      <IconButton>
        <DownloadIcon />
      </IconButton>
    ),
  },
];

const CreateComparison: React.FC = () => {
  return (
    <Container>
      <Box display="flex" justifyContent="space-between" my={4}>
        <Typography variant="h4">Quote Comparisons</Typography>
      </Box>
      <Grid container spacing={2}>
        <Grid item xs={12} md={6}>
          <Paper>
            <Box p={2} height={400}>
              <DataGrid rows={quoteComparisons} columns={columns} pageSize={5} />
            </Box>
          </Paper>
        </Grid>

        <Grid item xs={12} md={6}>
          <Paper>
            <Box p={2}>
              <Typography variant="h5">Create Comparison</Typography>
              <Box my={2}>
                <Button
                  variant="contained"
                  component="label"
                  startIcon={<UploadFileIcon />}
                >
                  Choose File
                  <input type="file" hidden />
                </Button>
              </Box>

              <Typography variant="subtitle1">Policy</Typography>
              <Box display="flex" alignItems="center" my={1}>
                <TextField
                  fullWidth
                  variant="outlined"
                  margin="normal"
                  InputProps={{ readOnly: true }}
                  value="ThisIs_theFilename_012543.pdf"
                />
                <Button variant="contained" color="secondary" sx={{ ml: 1 }}>
                  X
                </Button>
              </Box>

              <Typography variant="subtitle1">Quote(s)</Typography>
              {Array.from({ length: 5 }, (_, index) => (
                <Box key={index} display="flex" alignItems="center" my={1}>
                  <TextField
                    fullWidth
                    variant="outlined"
                    margin="normal"
                    InputProps={{ readOnly: true }}
                    value={`ThisIs_theFilenameAgainADuplicate_${index + 1}_012543.pdf`}
                  />
                  <Button variant="contained" color="secondary" sx={{ ml: 1 }}>
                    X
                  </Button>
                </Box>
              ))}

              <Box display="flex" justifyContent="space-between" my={2}>
                <Button variant="outlined" color="primary">
                  Clear All
                </Button>
                <Button variant="contained" color="primary">
                  Generate Comparison
                </Button>
              </Box>
            </Box>
          </Paper>
        </Grid>
      </Grid>
    </Container>
  );
};

export default CreateComparison;